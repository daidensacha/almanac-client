// src/contexts/EventsContext.jsx
export function useEventsContext() {
  throw new Error('useEventsContext is gone. Use React Query instead (e.g. useEvents).');
}

export default function EventsContextProvider() {
  throw new Error('EventsContextProvider is gone. Wrap nothing — use React Query instead.');
}
