// src/contexts/PlantsContext.jsx
export function usePlantsContext() {
  throw new Error('usePlantsContext is gone. Use React Query instead (e.g. useEvents).');
}

export default function PlantsContextProvider() {
  throw new Error('PlantsContextProvider is gone. Wrap nothing — use React Query instead.');
}
