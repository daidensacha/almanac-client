// src/contexts/CategoryContext.jsx
export function useCategoryContext() {
  throw new Error('useCategoryContext is gone. Use React Query instead (e.g. useEvents).');
}

export default function CategoryContextProvider() {
  throw new Error('CategoryContextProvider is gone. Wrap nothing — use React Query instead.');
}
